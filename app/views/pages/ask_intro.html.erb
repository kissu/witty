<div class="container">
  <div class="row larger-borders">
    <div class="col-xs-12">
      <div class="form-model">
        <h1 class="text-center report-title"><%= "#{t('.intro')} #{@contact.title}" %></h1>
        <p class="text-center" id="italic-under-title">
          <%= t('.email') %>
        </p>
        <%= render 'shared/mail_form', text: 'text_intro'%>
      </div>
    </div>
  </div>
</div>
