<h1>Contact</h1>

<%= render partial: "shared/button_tag", locals: { button: 'shuffle' } %>
<div class="products-container filtr-contact-container">
  <% @contacts.each do |contact| %>
    <%= render partial: "contacts/card", locals: { contact: contact } %>
  <% end %>
</div>
